# Rick and Morty Characters App

Мобильное приложение на Flutter для просмотра персонажей из мультсериала "Рик и Морти" с использованием [Rick and Morty API](https://rickandmortyapi.com).

## 📱 Функционал

### Основные возможности

- **Список персонажей** с пагинацией (автоматическая подгрузка при скролле)
- **Добавление в избранное** с анимацией
- **Оффлайн-режим** - кеширование загруженных данных
- **Сортировка избранного** по имени, статусу или виду
- **Темная тема** с возможностью переключения
- **Pull-to-refresh** для обновления списка

### Экраны

1. **Главный экран** - список всех персонажей
2. **Избранное** - список избранных персонажей с сортировкой

### Карточка персонажа содержит:

- Изображение персонажа
- Имя
- Статус (Alive/Dead/Unknown) с цветным индикатором
- Вид (Species)
- Локация
- Кнопка добавления в избранное (звездочка)

## 🏗️ Архитектура

Проект построен на основе **Clean Architecture** с разделением на слои:

```
lib/
├── core/                           # Общие компоненты
│   ├── domain/                     # Доменные модели
│   ├── infrastructure/             
│   │   ├── network/                # Dio провайдер
│   │   └── local/                  # Hive для локального хранения
│   └── presentation/               
│       ├── theme/                  # Темы приложения
│       └── widgets/                # Переиспользуемые виджеты
│
└── features/                       # Фичи приложения
    └── characters/
        ├── domain/                 # Модели (Character)
        ├── infrastructure/         
        │   ├── data_sources/       # API data source
        │   └── repos/              # Repository
        └── presentation/           
            ├── providers/          # Riverpod провайдеры
            └── screens/            # UI экраны
```

## 🛠️ Технологический стек

### State Management
- **Riverpod** (hooks_riverpod + riverpod_annotation) - управление состоянием
- Кодогенерация с `riverpod_generator`

### Сеть
- **Dio** - HTTP клиент
- **connectivity_plus** - проверка соединения

### Локальное хранилище
- **Hive** - NoSQL база данных для кеша и избранного
- **shared_preferences** - хранение настроек темы

### Модели
- **Freezed** - иммутабельные модели с копированием
- **json_serializable** - JSON сериализация

### UI
- **cached_network_image** - кеширование изображений
- **flutter_hooks** - упрощение работы с State

### Функциональное программирование
- **fpdart** - Either для обработки ошибок

## 📋 Требования

- Flutter SDK: **3.5.0** или выше
- Dart SDK: **3.5.0** или выше

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd testwork
```

### 2. Установка зависимостей

```bash
flutter pub get
```

### 3. Генерация кода

```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

### 4. Запуск приложения

```bash
flutter run
```

Или для конкретной платформы:

```bash
# Android
flutter run -d android

# iOS
flutter run -d ios

# Web
flutter run -d chrome
```

## 📦 Основные зависимости

| Пакет | Версия | Назначение |
|-------|--------|------------|
| hooks_riverpod | ^2.6.1 | State management |
| dio | ^5.8.0 | HTTP запросы |
| hive | ^2.2.3 | Локальная БД |
| freezed | ^2.4.1 | Генерация моделей |
| cached_network_image | ^3.3.0 | Кеширование изображений |
| fpdart | ^1.1.0 | Функциональное программирование |

Полный список зависимостей смотрите в [pubspec.yaml](pubspec.yaml).

## 🎨 Особенности реализации

### Пагинация
- Автоматическая подгрузка при достижении 80% списка
- Индикатор загрузки в конце списка

### Кеширование
- Все загруженные персонажи сохраняются в Hive
- При отсутствии интернета данные загружаются из кеша
- Изображения кешируются через `cached_network_image`

### Избранное
- Сохраняется в отдельном Hive box
- Не теряется при перезапуске приложения
- Доступно в оффлайн режиме

### Анимации
- Анимация звездочки при добавлении/удалении из избранного
- Scale анимация с эффектом "упругости" (elasticOut curve)

### Темная тема
- Автоматическое сохранение выбора темы
- Плавное переключение между темами
- Адаптивные цвета для обоих режимов

## 🧪 Тестирование

Запуск тестов:

```bash
flutter test
```

## 📱 Сборка для продакшена

### Android (APK)

```bash
flutter build apk --release
```

### Android (App Bundle)

```bash
flutter build appbundle --release
```

### iOS

```bash
flutter build ios --release
```

## 🔧 Разработка

### Генерация кода при изменениях

При изменении моделей, провайдеров или других файлов с кодогенерацией:

```bash
# Одноразовая генерация
flutter pub run build_runner build --delete-conflicting-outputs

# Автоматическая генерация при изменениях
flutter pub run build_runner watch --delete-conflicting-outputs
```

### Форматирование кода

```bash
dart format .
```

### Анализ кода

```bash
flutter analyze
```

## 📄 Структура кода

### Модели (Freezed)

```dart
@freezed
class Character with _$Character {
  const factory Character({
    required int id,
    required String name,
    required String status,
    // ...
  }) = _Character;

  factory Character.fromJson(Map<String, dynamic> json) =>
      _$CharacterFromJson(json);
}
```

### Провайдеры (Riverpod)

```dart
@riverpod
class CharactersList extends _$CharactersList {
  @override
  Future<List<Character>> build() async {
    // ...
  }
}
```

### Repository паттерн

- Data Source - прямое взаимодействие с API
- Repository - бизнес-логика, обработка ошибок, кеширование

## 🌐 API

Приложение использует [Rick and Morty API](https://rickandmortyapi.com/documentation):

- **Base URL**: `https://rickandmortyapi.com/api`
- **Endpoint**: `/character?page={page}`
- Пагинация: 10 персонажей на страницу

## 📝 TODO / Возможные улучшения

- [ ] Детальная страница персонажа
- [ ] Фильтрация по статусу и виду
- [ ] Поиск персонажей
- [ ] Локализация (en/ru)
- [ ] Unit тесты для Repository
- [ ] Widget тесты для экранов
- [ ] CI/CD pipeline

## 👨‍💻 Автор

Создано как тестовое задание для демонстрации навыков Flutter разработки.

## 📄 Лицензия

Данный проект создан в образовательных целях.

---

**Версия Flutter**: 3.5.0  
**Версия Dart**: 3.5.0  
**Дата создания**: 2026
